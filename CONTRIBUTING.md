# Bothrs Sidekick

## VScode marketplace

You need to install `vsce` (yarn global add vsce) to use the command line tools.

## Login

`vsce login bothrs`

## Publish

`vsce publish patch`
`vsce publish minor`
`vsce publish major`
